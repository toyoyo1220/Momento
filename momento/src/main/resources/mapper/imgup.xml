<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="project.momento.chart.mapper.ImgupMapper">	<!-- xml파일을 사용할 mapper가 있는 경로 -->

	<select id="marksChart" parameterType="project.momento.chart.dto.ChartDto" resultType="project.momento.chart.dto.ChartDto">
		<![CDATA[
		SELECT
			PK_PROD_SEQ,
			PROD_NAME,
			PROD_TYPE,
			PROD_KEYWORD,
			PROD_PRICE,
			REGIST_ID,
			REGIS_DT
		]]>
	</select>
	<!-- UPDATE문 -->
	
	<!-- DELETE문 -->
	
</mapper>